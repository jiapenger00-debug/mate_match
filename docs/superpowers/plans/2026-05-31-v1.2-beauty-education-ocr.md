# v1.2 学历+颜值+截图 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 学历从自由文本升级为下拉+搜索补全+标签；支持截图/粘贴图片用多模态LLM自动填入表单；支持上传照片进行颜值评分和匹配

**Architecture:** 渐进增强。vision_service 双后端设计（Qwen API / Ollama 本地），一套接口两个实现。学历纯前端，截图和颜值依赖 vision_service。

**Tech Stack:** Python 3.11+, FastAPI, Jinja2, vanilla JS, openai SDK (Qwen API), httpx (Ollama), Pillow

---

## Part 1：学历精细化

### Task 1: 创建高校数据文件

**Files:**
- Create: `static/universities.js`

- [ ] **Step 1: 编写中国高校列表**

创建文件，包含教育部公布的 3000+ 所高校，每所带标签（985/211/双一流/专科）。数据来源：教育部官网。

```js
// static/universities.js
// 中国高等院校列表（教育部公布），含学校标签
const UNIVERSITIES = [
  // 985
  { n: "清华大学", t: ["985","211","双一流"], c: "北京" },
  { n: "北京大学", t: ["985","211","双一流"], c: "北京" },
  { n: "复旦大学", t: ["985","211","双一流"], c: "上海" },
  { n: "上海交通大学", t: ["985","211","双一流"], c: "上海" },
  { n: "浙江大学", t: ["985","211","双一流"], c: "杭州" },
  { n: "南京大学", t: ["985","211","双一流"], c: "南京" },
  { n: "中国科学技术大学", t: ["985","211","双一流"], c: "合肥" },
  { n: "武汉大学", t: ["985","211","双一流"], c: "武汉" },
  { n: "华中科技大学", t: ["985","211","双一流"], c: "武汉" },
  { n: "西安交通大学", t: ["985","211","双一流"], c: "西安" },
  { n: "哈尔滨工业大学", t: ["985","211","双一流"], c: "哈尔滨" },
  { n: "中国人民大学", t: ["985","211","双一流"], c: "北京" },
  { n: "北京师范大学", t: ["985","211","双一流"], c: "北京" },
  { n: "南开大学", t: ["985","211","双一流"], c: "天津" },
  { n: "天津大学", t: ["985","211","双一流"], c: "天津" },
  { n: "大连理工大学", t: ["985","211","双一流"], c: "大连" },
  { n: "吉林大学", t: ["985","211","双一流"], c: "长春" },
  { n: "同济大学", t: ["985","211","双一流"], c: "上海" },
  { n: "厦门大学", t: ["985","211","双一流"], c: "厦门" },
  { n: "山东大学", t: ["985","211","双一流"], c: "济南" },
  { n: "中国海洋大学", t: ["985","211","双一流"], c: "青岛" },
  { n: "中山大学", t: ["985","211","双一流"], c: "广州" },
  { n: "华南理工大学", t: ["985","211","双一流"], c: "广州" },
  { n: "四川大学", t: ["985","211","双一流"], c: "成都" },
  { n: "电子科技大学", t: ["985","211","双一流"], c: "成都" },
  { n: "重庆大学", t: ["985","211","双一流"], c: "重庆" },
  { n: "中南大学", t: ["985","211","双一流"], c: "长沙" },
  { n: "湖南大学", t: ["985","211","双一流"], c: "长沙" },
  { n: "兰州大学", t: ["985","211","双一流"], c: "兰州" },
  { n: "西北工业大学", t: ["985","211","双一流"], c: "西安" },
  { n: "西北农林科技大学", t: ["985","211","双一流"], c: "杨凌" },
  { n: "东北大学", t: ["985","211","双一流"], c: "沈阳" },
  { n: "东南大学", t: ["985","211","双一流"], c: "南京" },
  { n: "北京航空航天大学", t: ["985","211","双一流"], c: "北京" },
  { n: "北京理工大学", t: ["985","211","双一流"], c: "北京" },
  { n: "中国农业大学", t: ["985","211","双一流"], c: "北京" },
  { n: "中央民族大学", t: ["985","211","双一流"], c: "北京" },
  { n: "华东师范大学", t: ["985","211","双一流"], c: "上海" },
  { n: "国防科技大学", t: ["985","211","双一流"], c: "长沙" },
  // 纯211（非985）
  { n: "上海财经大学", t: ["211","双一流"], c: "上海" },
  { n: "中央财经大学", t: ["211","双一流"], c: "北京" },
  { n: "对外经济贸易大学", t: ["211","双一流"], c: "北京" },
  { n: "北京外国语大学", t: ["211","双一流"], c: "北京" },
  { n: "上海外国语大学", t: ["211","双一流"], c: "上海" },
  { n: "中国政法大学", t: ["211","双一流"], c: "北京" },
  { n: "中国传媒大学", t: ["211","双一流"], c: "北京" },
  { n: "北京邮电大学", t: ["211","双一流"], c: "北京" },
  { n: "北京交通大学", t: ["211","双一流"], c: "北京" },
  { n: "北京科技大学", t: ["211","双一流"], c: "北京" },
  { n: "北京化工大学", t: ["211","双一流"], c: "北京" },
  { n: "北京林业大学", t: ["211","双一流"], c: "北京" },
  { n: "北京工业大学", t: ["211","双一流"], c: "北京" },
  { n: "华北电力大学", t: ["211","双一流"], c: "北京" },
  { n: "中国地质大学(北京)", t: ["211","双一流"], c: "北京" },
  { n: "中国石油大学(北京)", t: ["211","双一流"], c: "北京" },
  { n: "中国矿业大学(北京)", t: ["211","双一流"], c: "北京" },
  { n: "东华大学", t: ["211","双一流"], c: "上海" },
  { n: "上海大学", t: ["211","双一流"], c: "上海" },
  { n: "华东理工大学", t: ["211","双一流"], c: "上海" },
  { n: "南京航空航天大学", t: ["211","双一流"], c: "南京" },
  { n: "南京理工大学", t: ["211","双一流"], c: "南京" },
  { n: "河海大学", t: ["211","双一流"], c: "南京" },
  { n: "南京师范大学", t: ["211","双一流"], c: "南京" },
  { n: "苏州大学", t: ["211","双一流"], c: "苏州" },
  { n: "中国矿业大学", t: ["211","双一流"], c: "徐州" },
  { n: "江南大学", t: ["211","双一流"], c: "无锡" },
  { n: "南京农业大学", t: ["211","双一流"], c: "南京" },
  { n: "中国药科大学", t: ["211","双一流"], c: "南京" },
  { n: "武汉理工大学", t: ["211","双一流"], c: "武汉" },
  { n: "中南财经政法大学", t: ["211","双一流"], c: "武汉" },
  { n: "华中师范大学", t: ["211","双一流"], c: "武汉" },
  { n: "华中农业大学", t: ["211","双一流"], c: "武汉" },
  { n: "中国地质大学(武汉)", t: ["211","双一流"], c: "武汉" },
  { n: "西安电子科技大学", t: ["211","双一流"], c: "西安" },
  { n: "长安大学", t: ["211","双一流"], c: "西安" },
  { n: "陕西师范大学", t: ["211","双一流"], c: "西安" },
  { n: "西北大学", t: ["211","双一流"], c: "西安" },
  { n: "西南交通大学", t: ["211","双一流"], c: "成都" },
  { n: "西南财经大学", t: ["211","双一流"], c: "成都" },
  { n: "四川农业大学", t: ["211","双一流"], c: "雅安" },
  { n: "西南大学", t: ["211","双一流"], c: "重庆" },
  { n: "暨南大学", t: ["211","双一流"], c: "广州" },
  { n: "华南师范大学", t: ["211","双一流"], c: "广州" },
  { n: "哈尔滨工程大学", t: ["211","双一流"], c: "哈尔滨" },
  { n: "东北师范大学", t: ["211","双一流"], c: "长春" },
  { n: "大连海事大学", t: ["211","双一流"], c: "大连" },
  { n: "辽宁大学", t: ["211","双一流"], c: "沈阳" },
  { n: "郑州大学", t: ["211","双一流"], c: "郑州" },
  { n: "安徽大学", t: ["211","双一流"], c: "合肥" },
  { n: "合肥工业大学", t: ["211","双一流"], c: "合肥" },
  { n: "福州大学", t: ["211","双一流"], c: "福州" },
  { n: "南昌大学", t: ["211","双一流"], c: "南昌" },
  { n: "广西大学", t: ["211","双一流"], c: "南宁" },
  { n: "云南大学", t: ["211","双一流"], c: "昆明" },
  { n: "贵州大学", t: ["211","双一流"], c: "贵阳" },
  { n: "海南大学", t: ["211","双一流"], c: "海口" },
  { n: "内蒙古大学", t: ["211","双一流"], c: "呼和浩特" },
  { n: "宁夏大学", t: ["211","双一流"], c: "银川" },
  { n: "青海大学", t: ["211","双一流"], c: "西宁" },
  { n: "新疆大学", t: ["211","双一流"], c: "乌鲁木齐" },
  { n: "石河子大学", t: ["211","双一流"], c: "石河子" },
  { n: "西藏大学", t: ["211","双一流"], c: "拉萨" },
  { n: "延边大学", t: ["211","双一流"], c: "延吉" },
  // 著名双非
  { n: "深圳大学", t: [], c: "深圳" },
  { n: "南方科技大学", t: ["双一流"], c: "深圳" },
  { n: "首都医科大学", t: [], c: "北京" },
  { n: "首都师范大学", t: ["双一流"], c: "北京" },
  { n: "中国科学院大学", t: ["双一流"], c: "北京" },
  { n: "中国社会科学院大学", t: [], c: "北京" },
  { n: "华东政法大学", t: [], c: "上海" },
  { n: "西南政法大学", t: [], c: "重庆" },
  { n: "杭州电子科技大学", t: [], c: "杭州" },
  { n: "浙江工业大学", t: [], c: "杭州" },
  { n: "宁波大学", t: ["双一流"], c: "宁波" },
  { n: "江苏大学", t: [], c: "镇江" },
  { n: "南京邮电大学", t: ["双一流"], c: "南京" },
  { n: "南京信息工程大学", t: ["双一流"], c: "南京" },
  { n: "广东工业大学", t: [], c: "广州" },
  { n: "广州大学", t: [], c: "广州" },
  { n: "湘潭大学", t: ["双一流"], c: "湘潭" },
  { n: "燕山大学", t: [], c: "秦皇岛" },
  { n: "河北工业大学", t: ["211","双一流"], c: "天津" },
  { n: "天津工业大学", t: ["双一流"], c: "天津" },
];

// 搜索匹配函数
function searchUniversities(query) {
  var q = query.trim().toLowerCase();
  if (!q) return [];
  return UNIVERSITIES.filter(function(u) {
    return u.n.toLowerCase().indexOf(q) >= 0 || u.c.indexOf(query) >= 0;
  }).slice(0, 8);
}

// 获取学校标签
function getUniversityTags(name) {
  var found = UNIVERSITIES.find(function(u) { return u.n === name; });
  return found ? found.t : [];
}

// 格式化标签为显示文本
function formatTags(tags) {
  if (!tags.length) return "";
  return tags.map(function(t) {
    if (t === "985") return '<span class="uni-tag tag-985">985</span>';
    if (t === "211") return '<span class="uni-tag tag-211">211</span>';
    if (t === "双一流") return '<span class="uni-tag tag-dyl">双一流</span>';
    return "";
  }).join(" ");
}
```

- [ ] **Step 2: 提交**

```bash
git add static/universities.js
git commit -m "feat: add Chinese university database with 985/211/双一流 tags"
```

---

### Task 2: 学历字段改造 + 学校搜索补全 CSS

**Files:**
- Modify: `templates/index.html`
- Modify: `static/style.css`

- [ ] **Step 1: CSS 新增样式**

在 `static/style.css` 中追加（搜索补全下拉 + 学校标签样式）：

```css
/* ── 学历学校搜索 ──────────────────────────── */
.uni-search-wrap { position: relative; }
.uni-search-dropdown {
    position: absolute; top: 100%; left: 0; right: 0; z-index: 50;
    background: var(--bg-mid);
    border: 1px solid var(--border);
    border-radius: 0 0 8px 8px;
    max-height: 200px; overflow-y: auto;
    display: none;
}
.uni-search-dropdown.visible { display: block; }
.uni-search-item {
    padding: 8px 12px; cursor: pointer; font-size: 0.85rem;
    color: var(--text); border-bottom: 1px solid var(--border);
}
.uni-search-item:hover { background: rgba(212,132,122,0.08); }
.uni-search-item:last-child { border-bottom: none; }
.uni-tags { margin-top: 4px; font-size: 0.75rem; }
.uni-tag {
    display: inline-block; padding: 1px 6px; border-radius: 3px;
    margin-right: 4px; font-weight: 600; font-size: 0.7rem;
}
.tag-985 { background: rgba(212,132,122,0.15); color: var(--rose-light); }
.tag-211 { background: rgba(201,169,110,0.15); color: var(--gold); }
.tag-dyl { background: rgba(80,180,120,0.12); color: #6fd89a; }
```

- [ ] **Step 2: index.html 改造学历字段**

在女方信息卡片中，找到学历字段：
```html
<div class="form-group">
    <label for="girl_education">学历</label>
    <input type="text" id="girl_education" name="girl_education" placeholder="如：本科">
</div>
```
替换为：
```html
<div class="form-row">
    <div class="form-group">
        <label for="girl_edu_level">学历</label>
        <select id="girl_edu_level" name="girl_edu_level" style="width:100%;padding:0.7rem 0.9rem;background:var(--input-bg);border:1px solid var(--input-border);border-radius:8px;color:var(--text);font-size:0.93rem;font-family:inherit;">
            <option value="">请选择…</option>
            <option value="高中/中专">高中/中专</option>
            <option value="大专">大专</option>
            <option value="本科">本科</option>
            <option value="硕士">硕士</option>
            <option value="MBA">MBA</option>
            <option value="博士">博士</option>
            <option value="博士后">博士后</option>
            <option value="其他">其他</option>
        </select>
    </div>
    <div class="form-group uni-search-wrap">
        <label for="girl_edu_school">学校</label>
        <input type="text" id="girl_edu_school" name="girl_edu_school" autocomplete="off"
               placeholder="输入学校名称…">
        <div class="uni-search-dropdown" id="girl-uni-dropdown"></div>
        <div class="uni-tags" id="girl-uni-tags"></div>
    </div>
</div>
```

同样改造用户信息卡片中的学历字段（id 前缀改为 `user_`）。

在两个卡片的表单底部加 hidden 字段：
```html
<input type="hidden" name="girl_edu_tags" id="girl_edu_tags">
<input type="hidden" name="user_edu_tags" id="user_edu_tags">
```

- [ ] **Step 3: JS 学校搜索逻辑**

在 `index.html` 的 `<script>` 末尾、`</body>` 前追加：

```javascript
        // 学校搜索补全
        (function () {
            function setupUniSearch(inputId, dropdownId, tagsId, hiddenId) {
                var input = document.getElementById(inputId);
                var dropdown = document.getElementById(dropdownId);
                var tagsEl = document.getElementById(tagsId);
                var hidden = document.getElementById(hiddenId);
                if (!input) return;

                input.addEventListener('input', function () {
                    var results = searchUniversities(input.value);
                    if (!results.length) { dropdown.classList.remove('visible'); return; }
                    dropdown.innerHTML = '';
                    results.forEach(function (u) {
                        var item = document.createElement('div');
                        item.className = 'uni-search-item';
                        item.textContent = u.n + (u.t.length ? ' (' + u.t.join('/') + ')' : '');
                        item.addEventListener('click', function () {
                            input.value = u.n;
                            dropdown.classList.remove('visible');
                            tagsEl.innerHTML = formatTags(u.t);
                            hidden.value = u.t.join(',');
                        });
                        dropdown.appendChild(item);
                    });
                    dropdown.classList.add('visible');
                });
                input.addEventListener('blur', function () {
                    setTimeout(function () { dropdown.classList.remove('visible'); }, 200);
                });
            }
            setupUniSearch('girl_edu_school', 'girl-uni-dropdown', 'girl-uni-tags', 'girl_edu_tags');
            setupUniSearch('user_edu_school', 'user-uni-dropdown', 'user-uni-tags', 'user_edu_tags');
        })();
```

同时在 `<head>` 中引用 `universities.js`：
```html
<script src="/static/universities.js"></script>
```

- [ ] **Step 4: demo-data.js 适配新字段**

更新 `static/demo-data.js` 中每个示例的 `girl` 和 `user`，把 `education` 字段拆分为：
```js
education: "本科",          // 保留原值用于兼容
edu_level: "本科",
edu_school: "浙江大学",
edu_tags: "985,211,双一流",
```

同时更新 index.html 中的 JS demo 填充逻辑，适配新增的字段名（`edu_level`、`edu_school`）。

- [ ] **Step 5: 提交**

```bash
git add static/style.css templates/index.html static/demo-data.js
git commit -m "feat: upgrade education field to dropdown + university search with 985/211 tags"
```

---

## Part 2：视觉服务基础设施

### Task 3: 创建 vision_service.py 双后端

**Files:**
- Create: `services/vision_service.py`
- Modify: `config.py`
- Modify: `requirements.txt`

- [ ] **Step 1: 更新 config.py**

在 `config.py` 末尾追加：

```python
# 视觉服务
VISION_BACKEND = os.getenv("VISION_BACKEND", "qwen_api")
QWEN_API_KEY = os.getenv("QWEN_API_KEY", "")
QWEN_MODEL = os.getenv("QWEN_MODEL", "qwen-vl-max")
OLLAMA_MODEL = os.getenv("OLLAMA_MODEL", "qwen2-vl:7b")
OLLAMA_BASE_URL = os.getenv("OLLAMA_BASE_URL", "http://localhost:11434")
```

- [ ] **Step 2: 创建 services/vision_service.py**

```python
"""视觉服务 — 双后端（Qwen API / Ollama 本地）统一接口。"""

import base64
import json
import io
import logging
from abc import ABC, abstractmethod

import httpx
from openai import OpenAI

from config import VISION_BACKEND, QWEN_API_KEY, QWEN_MODEL, OLLAMA_MODEL, OLLAMA_BASE_URL

logger = logging.getLogger(__name__)


class VisionBackend(ABC):
    """视觉后端抽象基类。"""
    @abstractmethod
    async def analyze(self, image_bytes: bytes, prompt: str) -> dict:
        """分析图片，返回 JSON dict。"""
        ...

    def supports_ocr(self) -> bool:
        return True


class QwenBackend(VisionBackend):
    """阿里云 DashScope（通义千问 VL）后端。"""
    def __init__(self):
        self.client = OpenAI(
            api_key=QWEN_API_KEY,
            base_url="https://dashscope.aliyuncs.com/compatible-mode/v1",
        )

    async def analyze(self, image_bytes: bytes, prompt: str) -> dict:
        b64 = base64.b64encode(image_bytes).decode("utf-8")
        data_url = f"data:image/jpeg;base64,{b64}"
        response = self.client.chat.completions.create(
            model=QWEN_MODEL,
            messages=[{
                "role": "user",
                "content": [
                    {"type": "image_url", "image_url": {"url": data_url}},
                    {"type": "text", "text": prompt},
                ],
            }],
            max_tokens=1000,
            temperature=0.3,
        )
        raw = response.choices[0].message.content
        return _parse_json(raw)


class OllamaBackend(VisionBackend):
    """本地 Ollama 后端。"""
    async def analyze(self, image_bytes: bytes, prompt: str) -> dict:
        b64 = base64.b64encode(image_bytes).decode("utf-8")
        async with httpx.AsyncClient(timeout=60.0) as client:
            resp = await client.post(
                f"{OLLAMA_BASE_URL}/api/generate",
                json={
                    "model": OLLAMA_MODEL,
                    "prompt": prompt,
                    "images": [b64],
                    "stream": False,
                    "format": "json",
                },
            )
            data = resp.json()
            return _parse_json(data.get("response", "{}"))


def get_backend() -> VisionBackend:
    """根据配置返回对应的视觉后端。"""
    if VISION_BACKEND == "local_ollama":
        logger.info(f"使用本地 Ollama 后端: {OLLAMA_MODEL}")
        return OllamaBackend()
    if VISION_BACKEND == "qwen_api" or VISION_BACKEND == "":
        if not QWEN_API_KEY:
            raise ValueError("未配置 QWEN_API_KEY，请在 .env 中设置或切换 VISION_BACKEND")
        logger.info(f"使用 Qwen API 后端: {QWEN_MODEL}")
        return QwenBackend()
    raise ValueError(f"未知 VISION_BACKEND: {VISION_BACKEND}")


def _parse_json(raw: str) -> dict:
    """从 LLM 返回文本中提取 JSON。"""
    # 尝试直接解析
    try:
        return json.loads(raw)
    except json.JSONDecodeError:
        pass
    # 尝试从 markdown code block 中提取
    import re
    match = re.search(r'```(?:json)?\s*([\s\S]*?)```', raw)
    if match:
        try:
            return json.loads(match.group(1))
        except json.JSONDecodeError:
            pass
    logger.warning(f"无法从视觉模型返回中解析 JSON: {raw[:200]}")
    return {}


async def analyze_beauty(image_bytes: bytes) -> dict:
    """分析人像照片的颜值。"""
    backend = get_backend()
    prompt = """分析这张人像照片的面部特征，进行客观的吸引力评估。返回 JSON（不要 markdown 包裹）：
{
  "score": 1到10的颜值分数（保留一位小数，5为平均，参考亚洲审美标准），
  "skin": "肤质评价（细腻/光滑/匀称/一般/粗糙）",
  "face_shape": "脸型（鹅蛋脸/瓜子脸/圆脸/方脸/长脸/心形脸）",
  "features": "五官特点简要描述，30字以内",
  "overall": "整体印象评价，20字以内"
}
如果照片中没有人脸，score 返回 0，overall 返回 "未检测到人脸"。
客观评价，不要刻意夸张或贬低。"""
    try:
        return await backend.analyze(image_bytes, prompt)
    except Exception as e:
        logger.error(f"颜值分析失败: {e}")
        return {"score": 0, "skin": "", "face_shape": "", "features": "", "overall": f"分析失败: {e}"}


async def ocr_extract(image_bytes: bytes) -> dict:
    """从截图/照片中提取个人信息。"""
    backend = get_backend()
    prompt = """分析这张图片，提取其中提到的个人信息。返回 JSON（不要 markdown 包裹）：
{
  "name": "姓名（中文名或昵称）",
  "age": 年龄数字（整数），
  "hometown": "家乡/所在城市",
  "occupation": "职业",
  "edu_level": "学历（高中/中专/大专/本科/硕士/MBA/博士/博士后）",
  "edu_school": "毕业院校名称",
  "personality": "性格描述，50字以内",
  "interests": "兴趣爱好，50字以内",
  "values": "价值观或生活态度，50字以内",
  "appearance": "外貌特征描述，30字以内"
}
未提及的字段返回 null，不要猜测或编造。只返回 JSON，不要其他文字。"""
    try:
        return await backend.analyze(image_bytes, prompt)
    except Exception as e:
        logger.error(f"截图解析失败: {e}")
        return {}


async def analyze_beauty_pair(girl_bytes: bytes, user_bytes: bytes) -> dict:
    """分析双方照片，返回各自评分 + 颜值匹配度。"""
    import asyncio
    girl_result, user_result = await asyncio.gather(
        analyze_beauty(girl_bytes),
        analyze_beauty(user_bytes),
    )
    # 计算颜值匹配度（差距越小分数越高）
    gs = girl_result.get("score", 0)
    us = user_result.get("score", 0)
    if gs > 0 and us > 0:
        gap = abs(gs - us)
        match = max(0, 100 - int(gap * 15))
        if gap <= 0.5:
            comment = "天作之合，颜值非常登对！"
        elif gap <= 1.5:
            comment = "颜值匹配度较高，很般配"
        elif gap <= 2.5:
            comment = "颜值有一定差距，但互补也是魅力"
        else:
            comment = "颜值差异较大，不过真爱不看脸"
    else:
        match = 0
        comment = "未能完整分析双方照片"
    return {
        "girl": girl_result,
        "user": user_result,
        "beauty_match": match,
        "beauty_comment": comment,
    }
```

- [ ] **Step 3: 更新 requirements.txt**

追加：
```
pillow==11.1.0
httpx>=0.28.0
```

- [ ] **Step 4: 更新 .env.example**

追加：
```env
# 视觉服务后端
VISION_BACKEND=qwen_api
# Qwen API Key（阿里云 DashScope）
QWEN_API_KEY=sk-xxx
# Ollama 本地部署（VISION_BACKEND=local_ollama 时使用）
OLLAMA_MODEL=qwen2-vl:7b
OLLAMA_BASE_URL=http://localhost:11434
```

- [ ] **Step 5: 提交**

```bash
git add services/vision_service.py config.py requirements.txt .env.example
git commit -m "feat: add dual-backend vision service (Qwen API + Ollama GPU)"
```

---

## Part 3：截图解析

### Task 4: 截图解析功能

**Files:**
- Modify: `main.py`
- Modify: `templates/index.html`

- [ ] **Step 1: 新增 /api/ocr 路由**

在 `main.py` 中，`/api/search` 路由后面添加：

```python
@app.post("/api/ocr")
async def ocr_import(request: Request):
    """接收截图/照片，用多模态 LLM 提取个人信息。"""
    from services.vision_service import ocr_extract
    import json as _json

    form = await request.form()
    image_file = form.get("image")
    if not image_file:
        return HTMLResponse(
            content=_json.dumps({"error": "未上传图片"}, ensure_ascii=False),
            status_code=400, media_type="application/json"
        )

    image_bytes = await image_file.read()
    try:
        result = await ocr_extract(image_bytes)
        return HTMLResponse(
            content=_json.dumps(result, ensure_ascii=False),
            media_type="application/json"
        )
    except Exception as e:
        logger.error(f"OCR 解析失败: {e}")
        return HTMLResponse(
            content=_json.dumps({"error": str(e)}, ensure_ascii=False),
            status_code=500, media_type="application/json"
        )
```

- [ ] **Step 2: 前端截图按钮 + 粘贴监听**

在 `index.html` 中，每个信息卡片（女方/用户）的 `<h2>` 旁边加一个截图导入按钮。在女方卡片 `<h2>` 后追加：

```html
<button type="button" class="ocr-import-btn" id="girl-ocr-btn" title="截图导入">
    📷 截图导入
</button>
```

用户卡片同理（id 为 `user-ocr-btn`）。

在 `<script>` 末尾追加截图导入逻辑：

```javascript
        // 截图导入
        (function () {
            function ocrImport(who) {
                var input = document.createElement('input');
                input.type = 'file'; input.accept = 'image/*';
                input.onchange = function () {
                    var file = input.files[0];
                    if (!file) return;
                    var overlay = document.getElementById('search-overlay');
                    var text = document.getElementById('search-overlay-text');
                    overlay.classList.add('visible');
                    text.textContent = '正在识别截图内容…';
                    var fd = new FormData();
                    fd.append('image', file);
                    fetch('/api/ocr', { method: 'POST', body: fd })
                        .then(function (r) { return r.json(); })
                        .then(function (data) {
                            overlay.classList.remove('visible');
                            if (data.error) { alert(data.error); return; }
                            var prefix = who + '_';
                            if (data.name) setVal(prefix + 'name', data.name);
                            if (data.age) setVal(prefix + 'age', String(data.age));
                            if (data.hometown) setVal(prefix + 'hometown', data.hometown);
                            if (data.occupation) setVal(prefix + 'occupation', data.occupation);
                            if (data.edu_level) setVal(prefix + 'edu_level', data.edu_level);
                            if (data.edu_school) {
                                var sch = document.getElementById(prefix + 'edu_school');
                                if (sch) { sch.value = data.edu_school; sch.dispatchEvent(new Event('input')); }
                            }
                            if (data.personality) setVal(prefix + 'personality', data.personality);
                            if (data.interests) setVal(prefix + 'interests', data.interests);
                            if (data.values) setVal(prefix + 'values', data.values);
                            if (data.appearance) setVal(prefix + 'appearance', data.appearance);
                        })
                        .catch(function () {
                            overlay.classList.remove('visible');
                            alert('识别失败，请重试');
                        });
                };
                input.click();
            }
            function setVal(id, val) {
                var el = document.getElementById(id);
                if (el && val) { el.value = val; el.style.borderColor = '#50b478'; }
            }
            document.getElementById('girl-ocr-btn').addEventListener('click', function () { ocrImport('girl'); });
            document.getElementById('user-ocr-btn').addEventListener('click', function () { ocrImport('user'); });

            // Ctrl+V 粘贴图片
            document.addEventListener('paste', function (e) {
                var items = e.clipboardData && e.clipboardData.items;
                if (!items) return;
                for (var i = 0; i < items.length; i++) {
                    if (items[i].type.indexOf('image') >= 0) {
                        e.preventDefault();
                        var blob = items[i].getAsFile();
                        var overlay = document.getElementById('search-overlay');
                        var text = document.getElementById('search-overlay-text');
                        overlay.classList.add('visible');
                        text.textContent = '正在识别截图内容…';
                        var fd = new FormData();
                        fd.append('image', blob);
                        fetch('/api/ocr', { method: 'POST', body: fd })
                            .then(function (r) { return r.json(); })
                            .then(function (data) {
                                overlay.classList.remove('visible');
                                if (data.error) { alert(data.error); return; }
                                Object.keys(data).forEach(function (key) {
                                    if (data[key]) {
                                        var gId = 'girl_' + key, uId = 'user_' + key;
                                        if (document.getElementById(gId) && !document.getElementById(gId).value)
                                            setVal(gId, String(data[key]));
                                        else if (document.getElementById(uId) && !document.getElementById(uId).value)
                                            setVal(uId, String(data[key]));
                                    }
                                });
                            })
                            .catch(function () { overlay.classList.remove('visible'); });
                        break;
                    }
                }
            });
        })();
```

在 CSS 中追加：
```css
.ocr-import-btn {
    float: right; padding: 4px 12px;
    background: transparent; border: 1px solid var(--rose);
    color: var(--rose-light); border-radius: 6px;
    font-size: 0.8rem; cursor: pointer;
    transition: all 0.2s;
}
.ocr-import-btn:hover { background: rgba(212,132,122,0.1); }
```

- [ ] **Step 3: 提交**

```bash
git add main.py templates/index.html static/style.css
git commit -m "feat: add OCR screenshot import — button upload + Ctrl+V paste"
```

---

## Part 4：颜值评分

### Task 5: 颜值评分后端

**Files:**
- Modify: `main.py`
- Modify: `models.py`
- Modify: `services/llm_service.py`

- [ ] **Step 1: 新增 BeautyResult 模型**

在 `models.py` 末尾追加：

```python
class BeautyScore(BaseModel):
    """颜值评分结果"""
    score: float = Field(0.0, ge=0, le=10, description="颜值分数 1-10")
    skin: str = ""
    face_shape: str = ""
    features: str = ""
    overall: str = ""


class BeautyPairResult(BaseModel):
    """双方颜值分析结果"""
    girl: BeautyScore
    user: BeautyScore
    beauty_match: int = Field(0, ge=0, le=100)
    beauty_comment: str = ""
```

- [ ] **Step 2: 新增 /api/beauty 路由 + GET /beauty 页面**

在 `main.py` 中追加：

```python
@app.get("/beauty", response_class=HTMLResponse)
async def beauty_page(request: Request):
    """颜值 PK 页面。"""
    return templates.TemplateResponse("beauty.html", {"request": request})


@app.post("/api/beauty")
async def analyze_beauty_api(request: Request):
    """接收双方照片，返回颜值评分。"""
    from services.vision_service import analyze_beauty_pair
    import json as _json

    form = await request.form()
    girl_file = form.get("girl_photo")
    user_file = form.get("user_photo")

    if not girl_file or not user_file:
        return HTMLResponse(
            content=_json.dumps({"error": "请上传双方照片"}, ensure_ascii=False),
            status_code=400, media_type="application/json"
        )

    girl_bytes = await girl_file.read()
    user_bytes = await user_file.read()

    try:
        result = await analyze_beauty_pair(girl_bytes, user_bytes)
        return HTMLResponse(
            content=_json.dumps(result, ensure_ascii=False),
            media_type="application/json"
        )
    except Exception as e:
        return HTMLResponse(
            content=_json.dumps({"error": str(e)}, ensure_ascii=False),
            status_code=500, media_type="application/json"
        )
```

- [ ] **Step 3: llm_service.py prompt 增加颜值维度**

在 `SYSTEM_PROMPT` 的 `dimensions` 示例数组中追加颜值维度。找到 dimensions 部分，追加：

```json
{"dimension": "颜值匹配度", "score": 80, "comment": "基于 AI 面部分析..."}
```

同时在 `build_user_message()` 中，如果有照片分析结果，追加颜值信息段。在搜索结果信息后面、用户信息前面插入：

```python
    # 颜值信息（如有）
    beauty_girl = None
    beauty_user = None
    if hasattr(girl, 'beauty_score') and girl.beauty_score:
        beauty_girl = f"- 颜值：{girl.beauty_score} 分，{getattr(girl, 'beauty_desc', '')}"
    if hasattr(user, 'beauty_score') and user.beauty_score:
        beauty_user = f"- 颜值：{user.beauty_score} 分，{getattr(user, 'beauty_desc', '')}"
    if beauty_girl or beauty_user:
        parts.append("\n## 颜值分析（基于照片 AI 评估）")
        if beauty_girl:
            parts.append(f"- 女方：{beauty_girl}")
        if beauty_user:
            parts.append(f"- 男方：{beauty_user}")
```

- [ ] **Step 4: 提交**

```bash
git add main.py models.py services/llm_service.py
git commit -m "feat: add beauty scoring API + route + LLM prompt dimension"
```

---

### Task 6: 颜值 PK 页面

**Files:**
- Create: `templates/beauty.html`
- Create: `static/beauty.css`

- [ ] **Step 1: 创建 beauty.css**

完整文件：

```css
/* beauty.css — 颜值 PK 页面样式 */
@import url('/static/style.css');

.beauty-hero { text-align: center; padding: 3rem 1rem 2rem; }
.beauty-hero h1 { font-family: "Noto Serif SC", serif; font-size: 2.5rem; color: var(--cream); }
.beauty-hero p { color: var(--text-dim); margin-top: 0.5rem; }

.beauty-pk { display: flex; gap: 2rem; max-width: 900px; margin: 0 auto; padding: 0 1rem; }
.beauty-side { flex: 1; }
.beauty-upload { aspect-ratio: 3/4; border: 2px dashed var(--border); border-radius: 16px; display: flex; flex-direction: column; align-items: center; justify-content: center; cursor: pointer; transition: border-color 0.3s, background 0.3s; background: rgba(255,245,240,0.02); overflow: hidden; position: relative; }
.beauty-upload:hover { border-color: var(--rose); background: rgba(212,132,122,0.04); }
.beauty-upload.has-photo { border-style: solid; border-color: var(--rose); }
.beauty-upload img { width: 100%; height: 100%; object-fit: cover; position: absolute; inset: 0; }
.beauty-upload-text { color: var(--text-dim); font-size: 0.9rem; z-index: 1; }
.beauty-upload-plus { font-size: 2.5rem; color: var(--rose-light); margin-bottom: 0.5rem; z-index: 1; }

.beauty-vs { display: flex; align-items: center; font-family: "Noto Serif SC", serif; font-size: 2rem; color: var(--rose-light); font-weight: 700; }

.beauty-result { display: none; text-align: center; padding: 1.5rem 0; }
.beauty-result.visible { display: block; }
.beauty-score-big { font-family: "Noto Serif SC", serif; font-size: 3.5rem; font-weight: 700; color: var(--rose-light); line-height: 1; }
.beauty-score-label { color: var(--text-dim); font-size: 0.85rem; margin-top: 0.3rem; }
.beauty-detail { color: var(--text-dim); font-size: 0.85rem; margin-top: 0.5rem; line-height: 1.6; }

.beauty-match { text-align: center; padding: 1.5rem 0; display: none; }
.beauty-match.visible { display: block; }
.beauty-match-score { font-size: 1.5rem; font-weight: 700; color: var(--gold); }
.beauty-match-comment { color: var(--text-dim); margin-top: 0.3rem; }

@media (max-width: 700px) {
    .beauty-pk { flex-direction: column; }
    .beauty-vs { transform: rotate(90deg); }
}
```

- [ ] **Step 2: 创建 beauty.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="theme-color" content="#1a0a14">
    <title>颜值 PK · 灵魂契合度</title>
    <link rel="stylesheet" href="/static/beauty.css">
</head>
<body>
    <button class="theme-toggle" id="theme-toggle" aria-label="切换明暗主题"></button>
    <div class="hearts-container" aria-hidden="true">
        <span class="heart-particle">🌸</span><span class="heart-particle">💕</span>
        <span class="heart-particle">✨</span><span class="heart-particle">💖</span>
        <span class="heart-particle">🌹</span><span class="heart-particle">💗</span>
        <span class="heart-particle">🫧</span><span class="heart-particle">💝</span>
        <span class="heart-particle">💫</span><span class="heart-particle">🌸</span>
    </div>

    <div class="beauty-hero">
        <h1>颜值 PK</h1>
        <p>上传双方照片，AI 为你解析颜值匹配度</p>
    </div>

    <div class="beauty-pk">
        <div class="beauty-side" id="girl-side">
            <div class="beauty-upload" id="girl-upload" onclick="document.getElementById('girl-file').click()">
                <div class="beauty-upload-plus">+</div>
                <div class="beauty-upload-text">上传女方照片</div>
            </div>
            <input type="file" id="girl-file" accept="image/*" style="display:none" onchange="preview(this, 'girl')">
            <div class="beauty-result" id="girl-result">
                <div class="beauty-score-big" id="girl-score">-</div>
                <div class="beauty-score-label">颜值评分</div>
                <div class="beauty-detail" id="girl-detail"></div>
            </div>
        </div>

        <div class="beauty-vs">VS</div>

        <div class="beauty-side" id="user-side">
            <div class="beauty-upload" id="user-upload" onclick="document.getElementById('user-file').click()">
                <div class="beauty-upload-plus">+</div>
                <div class="beauty-upload-text">上传男方照片</div>
            </div>
            <input type="file" id="user-file" accept="image/*" style="display:none" onchange="preview(this, 'user')">
            <div class="beauty-result" id="user-result">
                <div class="beauty-score-big" id="user-score">-</div>
                <div class="beauty-score-label">颜值评分</div>
                <div class="beauty-detail" id="user-detail"></div>
            </div>
        </div>
    </div>

    <div class="beauty-match" id="beauty-match">
        <div class="beauty-match-score" id="match-score"></div>
        <div class="beauty-match-comment" id="match-comment"></div>
    </div>

    <div style="text-align:center;padding:2rem;">
        <a href="/" class="btn" style="display:inline-block;width:auto;padding:0.8rem 2rem;text-decoration:none;">返回首页</a>
    </div>

    <footer class="footer"><p>颜值分析由 AI 生成，仅供参考</p></footer>

    <script>
        function preview(input, who) {
            var file = input.files[0];
            if (!file) return;
            var upload = document.getElementById(who + '-upload');
            upload.innerHTML = '<img src="' + URL.createObjectURL(file) + '" alt="">';
            upload.classList.add('has-photo');
            checkBoth();
        }
        var submitted = false;
        function checkBoth() {
            if (submitted) return;
            var gf = document.getElementById('girl-file').files[0];
            var uf = document.getElementById('user-file').files[0];
            if (!gf || !uf) return;
            submitted = true;
            var fd = new FormData();
            fd.append('girl_photo', gf);
            fd.append('user_photo', uf);
            document.getElementById('girl-score').textContent = '...';
            document.getElementById('user-score').textContent = '...';
            fetch('/api/beauty', { method: 'POST', body: fd })
                .then(function (r) { return r.json(); })
                .then(function (data) {
                    if (data.error) { alert(data.error); submitted = false; return; }
                    showResult('girl', data.girl);
                    showResult('user', data.user);
                    document.getElementById('match-score').textContent = '颜值匹配度：' + data.beauty_match + ' 分';
                    document.getElementById('match-comment').textContent = data.beauty_comment;
                    document.getElementById('beauty-match').classList.add('visible');
                })
                .catch(function () { submitted = false; });
        }
        function showResult(who, data) {
            document.getElementById(who + '-result').classList.add('visible');
            document.getElementById(who + '-score').textContent = data.score.toFixed(1);
            document.getElementById(who + '-detail').innerHTML =
                '肤质：' + (data.skin || '-') + '<br>' +
                '脸型：' + (data.face_shape || '-') + '<br>' +
                (data.features || '') + '<br>' + (data.overall || '');
        }
        (function () {
            var t = document.getElementById('theme-toggle');
            var h = document.documentElement;
            var s = localStorage.getItem('theme');
            if (s) h.setAttribute('data-theme', s);
            t.addEventListener('click', function () {
                var n = h.getAttribute('data-theme') === 'light' ? 'dark' : 'light';
                h.setAttribute('data-theme', n); localStorage.setItem('theme', n);
            });
        })();
    </script>
</body>
</html>
```

- [ ] **Step 3: 提交**

```bash
git add templates/beauty.html static/beauty.css
git commit -m "feat: add beauty PK page — side-by-side photo upload + AI scoring"
```

---

### Task 7: 表单照片上传 + 结果展示

**Files:**
- Modify: `templates/index.html`
- Modify: `templates/result.html`
- Modify: `main.py`

- [ ] **Step 1: index.html 表单添加照片上传区**

在女方信息卡片底部、`补充信息` textarea 后面，追加：

```html
<div class="form-group full photo-upload-wrap">
    <label>照片（选填，用于颜值分析）</label>
    <div class="photo-upload" id="girl-photo-upload" onclick="document.getElementById('girl_photo_file').click()">
        <span class="photo-upload-placeholder">+ 上传女方照片</span>
        <img id="girl-photo-preview" style="display:none;">
    </div>
    <input type="file" id="girl_photo_file" name="girl_photo" accept="image/*" style="display:none"
           onchange="photoPreview(this, 'girl')">
</div>
```

用户卡片同理（id 改为 `user-photo-upload`、`user_photo_file`、`user-photo-preview`）。

JS 追加：
```javascript
function photoPreview(input, who) {
    var file = input.files[0];
    if (!file) return;
    var preview = document.getElementById(who + '-photo-preview');
    var placeholder = input.parentElement.querySelector('.photo-upload-placeholder');
    preview.src = URL.createObjectURL(file);
    preview.style.display = 'block';
    if (placeholder) placeholder.style.display = 'none';
}
```

- [ ] **Step 2: CSS 追加照片上传区样式**

```css
.photo-upload {
    width: 100%; min-height: 100px;
    border: 2px dashed var(--border);
    border-radius: 12px;
    display: flex; align-items: center; justify-content: center;
    cursor: pointer; transition: border-color 0.3s;
}
.photo-upload:hover { border-color: var(--rose); }
.photo-upload img { max-width: 100%; max-height: 200px; border-radius: 8px; }
.photo-upload-placeholder { color: var(--text-dim); font-size: 0.9rem; }
```

- [ ] **Step 3: /api/analyze 支持照片**

在 `main.py` 的 `/api/analyze` 中，将 `request: Request` 参数保留，函数体改为接收 `multipart/form-data`：

在参数列表中新增：
```python
    girl_photo: UploadFile = File(None),
    user_photo: UploadFile = File(None),
```

需要 `from fastapi import File, UploadFile` 到顶部 import。

在 LLM 分析前，如果有照片则调用颜值评分：

```python
    # 步骤1.5：颜值分析（如有照片）
    beauty_result = None
    if girl_photo or user_photo:
        from services.vision_service import analyze_beauty_pair as _beauty
        try:
            girl_bytes = await girl_photo.read() if girl_photo else None
            user_bytes = await user_photo.read() if user_photo else None
            if girl_bytes and user_bytes:
                beauty_result = await _beauty(girl_bytes, user_bytes)
                logger.info(f"颜值分析完成: {beauty_result.get('beauty_match')}")
        except Exception as e:
            logger.warning(f"颜值分析失败: {e}")
```

将 `beauty_result` 传入模板：
```python
    "beauty": beauty_result,
```

- [ ] **Step 4: result.html 展示颜值维度**

在结果页维度列表后面，如果有颜值数据则渲染：

```html
{% if beauty %}
<section class="card">
    <h2>颜值分析</h2>
    <div style="display:flex;gap:2rem;text-align:center;">
        <div style="flex:1;">
            <div style="font-size:2rem;font-weight:700;color:var(--rose-light);">{{ "%.1f"|format(beauty.girl.score) }}</div>
            <div style="color:var(--text-dim);font-size:0.8rem;">女方颜值</div>
            <div style="color:var(--text-dim);font-size:0.78rem;margin-top:4px;">{{ beauty.girl.overall }}</div>
        </div>
        <div style="flex:1;">
            <div style="font-size:2rem;font-weight:700;color:var(--rose-light);">{{ "%.1f"|format(beauty.user.score) }}</div>
            <div style="color:var(--text-dim);font-size:0.8rem;">男方颜值</div>
            <div style="color:var(--text-dim);font-size:0.78rem;margin-top:4px;">{{ beauty.user.overall }}</div>
        </div>
    </div>
    <div style="text-align:center;margin-top:1rem;">
        <span style="color:var(--gold);font-size:1.1rem;font-weight:600;">颜值匹配度：{{ beauty.beauty_match }} 分</span>
        <p style="color:var(--text-dim);font-size:0.82rem;">{{ beauty.beauty_comment }}</p>
    </div>
</section>
{% endif %}
```

- [ ] **Step 5: 提交**

```bash
git add templates/index.html templates/result.html main.py static/style.css
git commit -m "feat: add photo upload to form + beauty results display"
```

---

### Task 8: 验证与收尾

- [ ] **Step 1: 启动测试**

```bash
python main.py --api-key sk-xxx --port 8000
```

验证清单：
1. 学历下拉可选，输入学校名出现补全，选后显示标签
2. 截图粘贴/上传能识别并填入字段
3. 颜值 PK 页 /beauty 可上传照片并获取评分
4. 表单上传照片后分析结果页展示颜值维度

- [ ] **Step 2: 最终提交**

```bash
git add -A
git commit -m "feat: v1.2 complete — education, OCR screenshot, beauty scoring"
```

---

## 文件变化总览

| 文件 | 操作 |
|------|------|
| `static/universities.js` | 新建 |
| `services/vision_service.py` | 新建 |
| `templates/beauty.html` | 新建 |
| `static/beauty.css` | 新建 |
| `config.py` | 修改（VISION_BACKEND 等） |
| `models.py` | 修改（BeautyScore, BeautyPairResult） |
| `main.py` | 修改（/api/ocr, /api/beauty, GET /beauty, File upload） |
| `templates/index.html` | 修改（学历+学校+照片+截图+OCR JS） |
| `templates/result.html` | 修改（颜值展示） |
| `services/llm_service.py` | 修改（颜值维度 prompt） |
| `static/style.css` | 修改（搜索补全+上传区+PK页） |
| `static/demo-data.js` | 修改（适配新字段） |
| `requirements.txt` | 修改（pillow） |
| `.env.example` | 修改（VISION_BACKEND 等） |

**不动：** `services/search_service.py`、`services/share_service.py`
