# soul_match — 功能 TODO

> 产品功能路线图 · 按优先级排列

---

## 🔴 高优先级

- [ ] **丰富示例库** — "一键填充示例"支持多组不同风格的情侣案例
  - 示例 1（已有）：文艺女 × 理工男（林小曼 × 张浩然）
  - 新增：活泼外向 × 沉稳内敛
  - 新增：职场精英 × 自由职业
  - 新增：同行业 × 不同性格（如两个程序员）
  - UI 改为下拉菜单或轮播选择示例，而非固定一组

- [ ] **分析结果可分享** — 生成唯一链接，可分享给朋友看
  - 将分析结果存储到 JSON 文件或 SQLite
  - 生成短链接（`/r/<id>`）可公开访问
  - 分享按钮（复制链接 / 生成二维码）

- [ ] **搜索功能增强**
  - 搜索时展示进度（"正在搜索…" 加载动画）
  - 搜索结果可视化卡片（标题 + 摘要 + 网站图标）
  - 支持手动编辑/筛选搜索结果后再提交给 LLM

---

## 🟡 中优先级

- [ ] **历史记录**
  - 保存每次分析记录（本地 SQLite）
  - 历史列表页面：查看、删除、重新分析
  - 不依赖登录，纯本地存储

- [ ] **自定义分析维度**
  - 前端允许用户选择分析哪些维度（打勾性格/价值观/兴趣/生活/背景）
  - 或新增自定义维度（如"经济观念""家庭规划"）
  - 后端 `llm_service.py` 动态生成 prompt 维度列表

- [ ] **分步引导表单**
  - 替代当前长表单，改为 Step 1/2/3 向导式
  - 进度条 + 上一步/下一步按钮
  - 每步聚焦一个主题（基本信息 → 性格兴趣 → 价值观）

- [ ] **导出报告**
  - 导出为图片（HTML → Canvas → PNG）
  - 导出为 PDF
  - 报告页面增加 "下载报告" 按钮

---

## 🟢 低优先级

- [ ] **多模型支持**
  - 前端或配置中切换 LLM（DeepSeek / OpenAI / 通义千问 / 本地模型）
  - 预设几种模型的连接配置
  - 注意：DeepSeek 是默认，切换只需改 Base URL + Model

- [ ] **多语言支持**
  - 前端界面中/英切换
  - LLM prompt 跟随语言输出对应语言的分析报告

- [ ] **分析后追问**
  - 结果页底部增加对话输入框
  - 用户可以追问（如"我们适合异地恋吗"）
  - 调用 LLM 结合之前的分析上下文回答

- [ ] **加载体验优化**
  - 全屏加载动画（心形进度条或浪漫过渡动效）
  - 模拟分析步骤进度（搜索 → 分析 → 生成报告）

---

## 📊 进度

| 优先级 | 完成 | 总计 |
|--------|------|------|
| 🔴 高 | 0 | 3 |
| 🟡 中 | 0 | 4 |
| 🟢 低 | 0 | 4 |
| **全部** | **0** | **11** |
